{
  "name": "@unified/gateway",
  "version": "0.0.0",
  "private": true,
  "description": "nginx reverse proxy gateway for single-hostname Cloud Run session sync",
  "scripts": {
    "lint": "echo \"No lint for nginx gateway\"",
    "typecheck": "echo \"No typecheck for nginx gateway\"",
    "test": "echo \"No tests yet\"",
    "build": "echo \"Gateway is nginx; image built via Dockerfile\""
  }
}
